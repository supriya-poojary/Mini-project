services:
  - type: web
    name: speech-to-text-studio
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /healthz
    envVars:
      - key: GROQ_API_KEY
        sync: false
